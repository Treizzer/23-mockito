package com.treizer;

public class Main {
    public static void main(String[] args) {
        // PlayerRepository playerRepository = new PlayerRepository();
        // PlayerService playerService = new PlayerService(playerRepository);
        // PlayerController playerController = new PlayerController(playerService);

        // System.out.println(playerController.findAll());
        // System.out.println(playerController.findById(2L));

        // System.out.println(playerController.deleteById(1L));
        // System.out.println(playerController.findAll());

        // System.out.println(playerController.save(
        // new PlayerInsertDto("Luis Díaz", "Delantero", "Liverpool")));
        // System.out.println(playerController.findAll());
    }
}